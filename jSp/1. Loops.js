const names = ['Numan', 'Mehar', 'Ahmed', 'Naaz'];

// for(let snglName of names){
//     console.log(snglName);
// }

// names.map((snglName, ind, fullArr) => {
//     console.log(snglName, ind, fullArr);
// })

names.forEach((snglName, ind, fullArr) => {
    console.log(snglName, ind, fullArr);
})